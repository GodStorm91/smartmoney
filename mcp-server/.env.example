# SmartMoney MCP server config
# In Docker (production), backend is reachable on the compose network:
BACKEND_URL=http://backend:8000
# Local dev against a running backend:
# BACKEND_URL=http://localhost:8000

MCP_HOST=0.0.0.0
MCP_PORT=3000
MCP_PATH=/mcp
BACKEND_TIMEOUT=30
